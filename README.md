# HackCBRE-2023

Running the code:

    py -3 -m venv myenv

    .\myenv\Scripts\activate

    pip install flask
    pip install python-dotenv
    pip install pandas
    pip install flask-cors

    flask run

